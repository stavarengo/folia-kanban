# A nested subcard never surfaces under a search or filter

## What happens

A subcard with no `status` of its own is not a member of any column: `buildBoard` (`src/model/board.ts`) excludes every nested card from `board.columns` and puts it in `board.childrenOf[parent]` instead. `Column` filters only the paths it holds — `matchCard` runs over the column's own top-level bucket (`src/ui/Column.tsx`, the `globalFiltering` block) — and `SubcardGroup` renders `childrenOf` unfiltered, under whatever parent survived.

So a nested subcard is visible if and only if its parent matches the query, whatever the subcard itself says. It can carry a Blocked marker, an overdue date, unread comments or the very words you typed, and no filter will bring it up; conversely, a subcard that matches nothing still shows while its parent matches.

## How to reproduce

On the `feature-showcase` board, `Record the launch demo` is a subcard of `Plan the v1.0 launch` with no `status`, and it declares `blocked-by: ["[[Fix keyboard-drag focus bug]]"]`, so its tile shows *Blocked*.

- Click the **Blocked** chip (or type `is:blocked`): only `Draft the announcement post` is listed. `Record the launch demo` is gone, together with its parent, which does not match.
- Type `Record the launch demo` in the search box: nothing is found.
- Type `Plan the v1.0 launch`: the parent appears and the subcard comes back nested under it, still un-searched-for.

## Why it matters

The search box and the chips read as "show me everything on this board that is X". For nested work they silently mean "show me everything whose PARENT is X". A person who filters for what is blocked, overdue or unread gets an answer that quietly omits an entire class of cards, with nothing on screen saying a subtree was skipped — the failure mode that hides work rather than showing too much.

It also makes the sample vault's own tour slightly untrue: the relationships table in `examples/README.md` presents `Record the launch demo` as the "stated from the other end" blocked card, and the Blocked filter never shows it (that page now says so; the note goes away when this is fixed).

## Likely code areas

- `src/ui/Column.tsx` — `SubcardGroup` (renders `childrenOf` with no filter) and the `globalFiltering` block that filters top-level paths only
- `src/model/board.ts` — `childrenOf` / `columns` split, and `descendantsOf` (already walks the same tree)
- open question for whoever picks this up: should a matching subcard be lifted to the top level of some column while filtering, or should a non-matching parent be kept as a hollow container? Both change what "filtered board" means, so decide it before coding.

## Status (2026-08-28)

Implemented and merged on `main`. A match is now judged on a card's own merits at any depth, and a matching nested subcard is lifted to the top level of its column with a `↳ <parent>` reference. The commits, in order: `9c0083c` (*surface a matching nested subcard under a filter*), `486087e` (*address review findings on the filter-lift*), `fcfa9a7` (*let one helper decide what a filter actually shows* — `FilterVisibility` in `src/model/board.ts`), `d49939f` (*don't count a lane-only nested match in the search tally*), `46aa10f` (*pin the search tally to what the board actually draws*), `6007ec9` (*stop the search tally counting matches a collapse hides*), `b1dfc97` (*stop crediting a match no column will draw*) and `a747dca` (*pin the two filter rules nothing was asserting*).

Automated coverage lives in `test/board.test.ts` (the visibility helper and the tally) and `test/ui.test.tsx` (the lifted tile, its parent reference, and the toolbar tally).

One line in the body above is now out of date: `examples/README.md` was updated by `9c0083c` and no longer carries the caveat about the Blocked filter missing `Record the launch demo` — line 72 of that page describes the fixed behaviour, both cards answering `is:blocked` with the subcard lifted under a `↳ Plan the v1.0 launch` reference. That is documentation of the merged change, not of a verified one.

**Live verification in Obsidian has not been done.** The behaviour the sample vault's own tour now promises has never been seen in the real app.

## To finish

1. Run `pnpm dev:examplesVault`, open `examples/` as a vault in Obsidian with the plugin enabled, and open `examples/feature-showcase/Showcase Board.md`.
2. Press `/` and type `is:blocked` (or click the **Blocked** chip). **Pass:** both `Draft the announcement post` and `Record the launch demo` are listed, and `Record the launch demo` appears as a top-level tile in its column carrying a `↳ Plan the v1.0 launch` reference — its parent does not itself match. **Fail:** only `Draft the announcement post` shows.
3. Type `Record the launch demo` in the search box. **Pass:** the subcard is found and lifted the same way.
4. With that filter active, compare the toolbar's match tally against what is drawn. **Pass:** the number equals the tiles actually visible on the board — count them.
5. Collapse a column's subitems (`⋯` → **Collapse all subitems**) with a filter active. **Pass:** the tally still matches what is drawn; a match hidden by the collapse is not counted.
6. Type `Plan the v1.0 launch`. **Pass:** the parent appears with its subcard nested underneath, as before.
7. Clear the filter (Escape or the clear button). **Pass:** the board returns to its unfiltered layout — `Record the launch demo` is nested under its parent in **Todo** again, with no leftover reference line or empty lifted tile.
8. Check the ⭐ **A-priority lane** while filtering: a nested match should not be credited to a lane that will not draw it.

Close it out: delete this entry when every step passes, and check whether `20260828.04`'s "Related work" paragraph needs rewording once it does; otherwise record the failure here.
