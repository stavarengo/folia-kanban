---
related:
  - overlapsWith: "[20260829.01 - The MCP server only listens on loopback, so a client in a container cannot reach it](./20260829.01.mcp-server-cannot-be-reached-from-a-container.md)"
---

# Expose the board to agents through an MCP server inside the plugin

## What is asked

A user-enabled flag that turns on an MCP server hosted inside the Folia Kanban plugin, so any agent can drive a board the way a person drives the UI: create cards, set fields, move things, everything that is possible by hand.

The motivation is concrete and current. Agents driving a board today edit the card files directly, and they do not follow the plugin's rules. Cards end up without their history lines because the agent never knew a history entry was owed. The point of the MCP is that the rules stop depending on the agent knowing them.

The architectural requirement: one implementation behind both entry points. A manual edit through the UI and an MCP request must run the same code, not two copies of the same logic.

One line of the request reads two ways. It asks for agents to stop editing files directly, and also for "editing the files and everything else" to be exposed through the MCP. Most likely that means the MCP can perform anything the UI can, including operations that rewrite file content, rather than offering a raw write-this-file tool. Worth confirming before anyone builds it.

## What already exists

The seam he is asking for is largely already there. `CardRepository` in `src/model/repo.ts` is described in its own header as "the contract the UI depends on": `VaultRepo` implements it against Obsidian, the tests implement it in memory, and the UI never touches the vault directly. What is missing is a second caller of that contract, not an architecture to put behind it.

The seam is not complete, though, and this is the sharp part. `App.tsx` composes the pure `moveCard` reducer from `board.ts` with `repo.applyMove` (lines 154, 187 and 296), so the fractional-order computation for a move lives in the UI layer, above the contract. A caller that reached only `CardRepository` would not inherit it.

The missing history has two causes, not one. `history.ts` owns whether a given event is allowed to emit a line and the adapter owns when to write it, and the default scope is `moves` — under which field edits, comments and subtask changes emit nothing at all. So part of what Rafa is seeing is agents bypassing `VaultRepo` entirely, and part is the configured scope. A fix aimed at only one half will look broken.

The plugin cannot prevent direct file edits. Obsidian's own plugin-security documentation states that plugins inherit the app's access level and that Obsidian cannot reliably restrict them; nothing gives a plugin a lock on the vault. The board's live-reload echo guard exists precisely because outside edits happen. "Agents are not allowed to edit files directly" is therefore a convention Rafa enforces on his agents, not something the plugin can enforce on them.

`manifest.json` declares `isDesktopOnly: false` and the README promises desktop and mobile. Hosting a server inside the plugin has to reckon with that promise.

This shape is proven in the ecosystem. The [Local REST API with MCP](https://github.com/coddingtonbear/obsidian-local-rest-api) community plugin already ships an MCP server that runs inside Obsidian, over Streamable HTTP on `127.0.0.1:27124`, authenticated with a bearer token and a self-signed certificate. Community plugins doing this are accepted in the store, under the Developer Policies and behind Restricted Mode as the user-facing gate.

## Undecided

- Which operations the MCP exposes, given that some `CardRepository` members (`renderMarkdown`, `openCard`) mean nothing to an agent.
- Where the move and ordering logic now living in `App.tsx` has to sit so both callers genuinely share it.
- Transport, authentication, and what the enable flag defaults to.
- Whether the flag is per-vault or per-board, and how an agent picks a board when the vault has several.
- Whether the server runs while no board view is open, and what happens on mobile, where the plugin currently claims support.
- Whether history and the echo guard behave correctly when writes originate outside the UI.
- Whether the MCP surface becomes a documented, versioned public contract, given that this is a published community plugin.

## Status (2026-08-28)

Implemented and merged on `main`. `e943a30` (*refactor(model): move the card-move composition below the repository port*) first moved the move-and-ordering logic out of `App.tsx` into `src/model/boardOps.ts`, so both callers genuinely share it. `716022e` (*feat(mcp): let agents drive a board through the plugin instead of around it*) added the server itself — off by default, loopback only, bearer token, nine tools. Then `2e48004` (*close the gaps a first review found*), `aaf3489` (*stop the tools misreporting writes that actually happened*), `00af970` (*pin why a reported column cannot follow a subcard cycle*), `0db0b3f` (*keep the agent's write worth the same as a person's*), `4545145` (*stop an agent writing the board's own record through the tools*) and `6e2f4a3` (*keep the one-call-at-a-time promise while still freeing the client*).

It is documented in `docs/mcp.md`, which is the reference for settings, the connection command and every tool.

Automated coverage lives in `test/mcp.test.ts` (the tools), `test/mcpHttp.test.ts` (transport, auth, the queue and its timeout), `test/mcpService.test.ts` (enable/disable and token lifecycle), `test/mcpContract.test.ts` with `test/__snapshots__/mcpContract.test.ts.snap` (the frozen tool surface, checked by `pnpm contracts:check`) and `test/boardOps.test.ts` (the shared move logic).

**Live verification in Obsidian has not been done.** No client has ever connected to the server running inside a real Obsidian process: everything above was proved against the HTTP layer in tests.

## Status (2026-08-29)

The first live attempt, from a session driving Obsidian 1.12.7 on the host through the Chrome DevTools bridge, got as far as step 6 and found one real defect on the way.

**The server could not start at all: `Failed to resolve module specifier 'http'`.** `src/obsidian/mcpHttpServer.ts` loads Node's `http` with a dynamic `import()` on purpose, so mobile never touches it. With `format: "cjs"` and `target: "es2018"`, esbuild left that external import as a native `import("http")` in the bundle, which Electron's renderer cannot resolve for a Node builtin. None of the tests could see this because they run under Node, where a native `import("http")` works. Fixed in `esbuild.config.mjs` with `supported: { "dynamic-import": false }`, which makes esbuild emit `require("http")`; the fix is committed on `main` with this note. After a rebuild and a plugin reload the server came up on `127.0.0.1:27125`: `tools/list` returned the nine tools with the token and `401` without it, and a `claude -p` session outside Obsidian called `list_boards` successfully.

Two corrections to the checklist below, learned from the same run:

- Step 6 says "the four example boards". The vault has six board notes, because `title-modes/` holds three boards over one card folder. Six is the pass condition.
- Steps 4 and 5 assume the client runs on the same machine as Obsidian. That is only because `MCP_HOST` is hardcoded to `127.0.0.1`; a client in a container cannot reach the host's loopback. The session that found this bridged the gap with an in-memory TCP relay opened inside Obsidian's own process (a Node `net` server bound to the container gateway `10.254.254.1:27125`, piping to loopback), which disappears on plugin reload. The durable answer is a bind-address setting, tracked in `20260829.01`. Until that exists, a container session needs either that relay or a `socat` on the host, and the `claude mcp add` command in step 5 must use the gateway address instead of `127.0.0.1`. To keep `~/.claude.json` clean, that session used `claude -p --strict-mcp-config --mcp-config <scratch file>` instead of `claude mcp add`.

Steps 7 to 12 have not been run yet.

## To finish

1. From the repo root, run `pnpm dev:examplesVault` and leave it running. Open `examples/` as a vault in Obsidian and enable **Folia Kanban**.
2. Settings → Folia Kanban → turn on **Agent access (MCP) — enable**. **Pass:** no error notice; if the port is taken the notice names it, so pick a free port in **Agent access (MCP) — port** and retry.
3. Click **Copy token**.
4. In a terminal *on the same machine* (not in a container — this is what blocked the previous attempt), sanity-check the server:

   ```sh
   curl -s -X POST http://127.0.0.1:27125/mcp \
     -H "Authorization: Bearer PASTE_YOUR_TOKEN_HERE" \
     -H "content-type: application/json" \
     -d '{"jsonrpc":"2.0","id":1,"method":"tools/list"}'
   ```

   **Pass:** a JSON reply listing the nine tools. **Fail:** connection refused (server not listening) or `401` (wrong token).
5. Register it with Claude Code:

   ```sh
   claude mcp add --transport http folia-kanban http://127.0.0.1:27125/mcp \
     --header "Authorization: Bearer PASTE_YOUR_TOKEN_HERE"
   ```

6. From a Claude Code session, call `list_boards`. **Pass:** the four example boards appear, including `feature-showcase/Showcase Board.md`.
7. Call `get_board` on `feature-showcase/Showcase Board.md`. **Pass:** the columns come back in board order, `Record the launch demo` is listed under its parent's `children` rather than in a column, and the ⭐ A-priority lane carries the `lane` note explaining that the listing is the status bucket.
8. Call `move_card` to move an ordinary card (e.g. `Draft the announcement post`) to another column. **Pass:** the tile moves on the board in Obsidian without a reload, and the card's note gains a `## History` line in exactly the shape a drag produces — compare it against a line written by dragging a card by hand.
9. Call `add_comment` on the same card. **Pass:** it lands under `## Comments` as one list item, timestamped and signed with the **Your name** setting, identical in shape to a comment typed in the panel.
10. Try one refusal to confirm the guards are live in the real app: `update_card` with `properties: { status: "done" }`. **Pass:** a tool error naming the tool that owns `status`, and the note is unchanged.
11. Switch **History — what to record** to `moves` and repeat step 9. **Pass:** no history line for the comment, matching what a hand-written comment does under that setting.
12. Turn agent access off again and confirm the port stops answering; turn it back on and confirm the same token still works.

Close it out: delete this entry when the run above passes; otherwise record which call failed and what came back.
