---
status: next
order: 3
priority: C
---

# Write the changelog

Group entries under **Added / Changed / Fixed**, pulled from merged PRs since 0.1.

_Subcard of [[Plan the v1.0 launch]] with a `status` of its own, so it stands in **Next Up** rather than nested under its parent. Its tile carries a `↳ Plan the v1.0 launch` reference instead of the nesting. Drag it back to Todo and it rejoins its parent's group; drag it into Done and the parent's `- [ ] [[Write the changelog]]` line is ticked for you, so the parent's progress counts it._
