---
status: todo
order: 3
priority: B
tags: perf
---

# Tune the search index

Profile the haystack build and cache what the `/` search re-computes on every keystroke. The follow-up is #profiling — a tag written here in the body rather than in the `tags` property, and `tag:profiling` finds this card by it just the same.

The file name is a numbered slug, so the board shows this card's first heading instead. Rename it on the board and the `# ` line is what gets rewritten — the file stays `04-tune-the-search-index.md`.
