---
status: next
order: 5
priority: C
title: Cut the 1.0 release notes
---

# Ship the release notes

Collect the changelog entries, write the highlights, and attach the demo GIF.

A `title:` in the card's own frontmatter wins over everything else, so the tile reads *Cut the 1.0 release notes* while the file keeps its own name. Renaming on the board rewrites that `title:` value.
