---
type: task
priority: B
created: 2026-01-12
---

# Write release notes

A subcard of "Plan the launch". Summarize what's new for v1.

It has no `status` of its own, so it lives wherever its parent card is — the default for every subitem. Give it one (or drag it) and it stands in that column instead.
