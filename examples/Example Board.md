---
kanban-board: true
card-folder: examples/Cards
columns:
  - todo
  - doing
  - done
---

# Example Board

A minimal board to try the plugin. Copy the `examples/` folder into your vault, then run **“Open Kanban board”**. Cards live in `examples/Cards`; columns are the `columns` list above.
