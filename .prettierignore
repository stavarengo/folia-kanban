# Build output and deps
dist/
node_modules/
.pnpm-store/
coverage/

# Example Obsidian vault — user content, not source
examples/

# Markdown is owned by markdownlint-cli2 (docs:check), not Prettier
**/*.md

# Hand-maintained design-system stylesheet is owned by ds:check
# (theme:check + portals:check), not Prettier
src/theme/*.css

# Lockfile
pnpm-lock.yaml

# Harness state and sibling worktrees, excluded from git only through .git/info/exclude, which Prettier does not read
.claude/
